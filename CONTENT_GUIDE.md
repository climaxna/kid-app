# 블로그 콘텐츠 작성 가이드

이 문서는 `wando/`, `jeju/` 등에 새 `.md` 파일을 쓸 때 지켜야 할 규칙이다.
새 세션(로컬/모바일 어느 쪽이든)에서 글을 쓰기 전에 먼저 이 문서를 읽는다.

---

## 0. 사실 검증 규칙 (제일 중요, 예외 없음)

**분위기·묘사적 디테일은 반드시 실제 웹 확인 후에만 쓴다.**

- "시골 할머니 집 같다", "언덕 위에 있다", "통창이 예쁘다" 같은 감각적 묘사는
  실제 리뷰·기사에 그 표현(혹은 명확히 같은 사실)이 있을 때만 쓴다.
  근거 없이 분위기를 지어내지 않는다.
- 상호명은 **네이버지도 검색으로 실존 여부를 교차 확인**한다 (아래 3번 참고).
  다이닝코드·블로그 글 하나만 보고 확정하지 않는다.
- 정부/공공기관 사이트(예: `wando.go.kr`)처럼 **JS로 데이터를 그리는 동적 사이트는
  WebFetch만으로 신뢰하지 않는다.** WebFetch는 실제 데이터가 없어도 그럴듯한
  내용을 지어낼 수 있다(과거 "청산도식당" 오류 사례). Playwright로 실제 렌더링해서
  확인하거나, 네이버지도/다이닝코드처럼 구조화된 실제 데이터를 주는 곳을 우선한다.
- 가격·영업시간처럼 자주 바뀌고 검증이 약한 숫자는 **지어내지 않는다.**
  확인 안 되면 "정확한 가격·영업시간은 방문 전 확인 권장" 문구로 대체한다.
  (반대로, 실제로 확인된 숫자가 있으면 얼버무리지 말고 그대로 쓴다 — 예: 짜장보고
  짜장면 9,000원, 탕수육 중 25,000원은 다이닝코드+네이버지도 리뷰로 교차 확인된 값.)

## 1. 콘텐츠 타입별 문체

타입마다 화자·톤·구조가 다르다. 섞지 않는다.

### 1-1. 여행 계획서 시리즈 (`wando-island-vol*.md`, `pohang-ulleungdo-*.md`, `jeju-island-*.md`)

- 화자: 40대 아빠, 쌍둥이(아직 어림) + 7살 첫째. "우리 애들 데리고" 톤.
- 구조: 제목 1줄 → 사진 플레이스홀더(`📷 [사진 N] 검색어: \`키워드\` → 설명` +
  다음 줄 `(ⓒ한국관광공사 포토코리아-촬영자명)`) → 도입 → `────` 구분선 →
  가는 방법 → 포인트 3곳 → 추천 코스표 → 아이 팁 → 경비표 → ferrycast 링크 →
  해시태그.
- 사진은 관광사진(한국관광공사) 자동 검색·삽입 대상 → 위 플레이스홀더 형식을
  그대로 지킨다 (naver_blog_draft.py가 이 패턴을 파싱함).
- 여기서만 "ㅋㅋ", 감성적 회고 톤을 쓴다.

### 1-2. 맛집 리뷰 (`*-food-vol*.md`)

여행 계획서와 다른 화자다. "오늘 다녀왔어요" 개인 방문 후기 톤.
**사진은 사용자가 직접 준비** → 플레이스홀더는 검색어 없이 `📷` 단독으로,
문장 1~2줄마다 하나씩 넣는다 (밀도를 높게 — 벤치마크 인플루언서 블로그 참고).

두 하위 타입 중 하나를 반드시 고른다 (섞지 않는다):

| 구분 | 화자/톤 | 제목 스타일 | 예시 |
|---|---|---|---|
| 가족용 | 편안, 안전, 아이/부모 동반 강조 | "○○맛집 상호명 메뉴들, 분위기" | 미로식당 |
| 젊은층/이슈용 | 짧고 강렬, 반전·후킹, 감탄사 | "이거 안 먹으면 후회", "실화냐" 류 | 짜장보고 |

- 상호명 + 메뉴 + 가격(확인된 것만) 정보 박스를 반드시 넣는다.
- **섬에 있는 맛집이면 ferrycast.kr 링크를 본문에 반드시 넣는다**
  (배편 확인 유도 — 이게 이 시리즈의 존재 이유다. 빠뜨리지 말 것).
- 해시태그에 지역명 + 상호명 + 메뉴명 포함.

### 1-3. FerryCast 자체 업데이트/공지 (`ferrycast-update-*.md`)

- 화자: 서비스 운영자, "저희 서비스는" 톤. 과장 없이 담백하게.
- 실제 스크린샷 이미지를 그대로 삽입 (관광사진 아님 — 직접 준비된 이미지 사용).
- 각주로 데이터 출처(MTIS·기상청·KHOA) 명시.

### 1-4. 기상/전문 지식 (`monsoon-*.md`, `typhoon-*.md`)

- 화자: 기상학 전문가. 실존 인물/기관을 사칭하지 않고 "기상학을 오래 들여다본
  사람"처럼 익명화된 전문가 화자를 쓴다.
- 원리 설명 → 근거 → 실전 팁 순서. 연도별 구체 날짜(장마 시작일 등)는 단정하지
  않고 실시간 확인을 안내한다.
- ferrycast.kr 링크 포함 (배편 결항과 연결되는 경우).

### 1-5. 육아용품 내돈내산 리뷰 (`baby/<제품>-vol*.md`)

- 화자: 다둥이 아빠(첫째 7살 남아 + 쌍둥이). 직접 사서 쓴 "내돈내산" 솔직 후기 톤.
  "쌍둥이라 하루에 분유를 십수 번" 같은 다둥이 현실 앵글을 살린다.
- 참고 스타일: 네이버 육아 블로거 호준맘(blog.naver.com/oburi-) 구조.
  - 본문 맨 위에 제목을 2회가량 반복 + 핵심 해시태그 3개 정도 배치(네이버 관행).
  - 소제목은 질문·감탄형 자연어("○○를 선택한 이유", "세척, 생각보다 간단해요!").
  - 짧은 줄바꿈(문장 1~2줄), `~더라구요/~했어요` 따뜻한 존댓말, 감탄사 적당히.
  - 사진은 **사용자가 직접 준비** → `📷` 단독 플레이스홀더를 문장 1~2줄마다 배치(고밀도).
- **사양·수치·세척 절차는 반드시 웹에서 확인한 것만 쓴다** (0번 규칙). 제품 스펙은
  공식 사이트/검증된 후기로 교차 확인하고, 가격은 본인이 실제 지불한 값을 모르면
  숫자를 지어내지 않는다.
- 여행/섬과 무관한 글이지만 **맨 끝에 ferrycast.kr 홍보 푸터를 넣는다**
  (같은 블로그이므로 운영자 서명처럼: "완도·섬 여행 계획 있으시면 저희 사이트도" 톤).

## 2. 공통 규칙

- 표(요금표, 코스표)는 마크다운 `| a | b |` + `|---|---|` 형식 그대로 쓴다
  (naver_blog_draft.py가 실제 네이버 표 컴포넌트로 자동 변환함).
- `http(s)://` URL은 자동으로 클릭 가능한 링크로 변환되니 그대로 쓰면 된다.
- 없는 사실을 만들어내는 것보다 "확인 필요" 문구를 쓰는 게 항상 낫다.

## 3. 상호명·정보 검증 절차 (맛집 글 전용)

새 맛집을 쓰기 전에 반드시 이 순서로 확인한다.

1. WebSearch로 1차 조사 (대략적인 메뉴·특징 파악용, 확정 근거로 쓰지 않음)
2. **네이버지도 실제 검색으로 실존 여부 교차 확인**
   (`node scripts/check_naver_map.mjs "상호명 지역명"` 패턴 — 스크립트가 없으면
   Playwright로 `https://map.naver.com/p/search/<검색어>`를 렌더링해서 확인.
   `searchIframe` 안에 실제 리뷰·평점이 있으면 실존 확정, 없으면 해당 상호는 쓰지 않는다.)
3. 리뷰 텍스트에 실제로 언급된 메뉴·특징·분위기만 글에 반영한다.
4. 가격·영업시간은 리뷰/다이닝코드에서 직접 확인된 것만 숫자로 쓴다.

## 4. 파일 위치 규칙

- 완도: `wando/wando-island-vol*.md`
- 포항-울릉도-독도: `wando/pohang-ulleungdo-vol*.md`
- 제주 부속섬: `jeju/jeju-island-vol*.md`
- 섬별 맛집: `wando/<섬이름>-food-vol*.md` (가족용 vol1, 이슈용 vol2 순서 권장)
- FerryCast 공지: `wando/ferrycast-update-*.md`
- 육아용품 리뷰: `baby/<제품명>-vol*.md`
