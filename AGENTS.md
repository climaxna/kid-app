# 블로그 프로젝트 공통 지침

이 작업 공간에서 사용자가 블로그 글 작성, 이미지 제작, 네이버 임시저장을 요청하면
`.agents/skills/naver-blog-pipeline/SKILL.md`의 통합 작업 흐름을 사용한다.

- 작업 시작 시 `BLOG_PIPELINE_CONFIG.json`, `BLOG_STRATEGY.md`, `CONTENT_GUIDE.md`,
  `IMAGE_STYLE_PRESETS.md`를 읽는다.
- 육아 글은 `PARENTING_EXPERIENCE_PROFILE.md`도 읽는다.
- 사용자가 주제만 주어도 조사, 원고 작성, 이미지 생성·검수, 이미지가 포함된 네이버
  임시저장까지 진행한다.
- 이미지 생성용 내부 브리프와 매니페스트는 네이버 본문에 넣지 않는다.
- 보편적인 육아 사례를 사용자의 실제 경험처럼 단정하지 않는다. 사용자 검토용 경험 문단은
  `<!-- experience -->` 내부 표시 다음 줄의 인용문으로 배치하되, 네이버 본문에는
  `경험담 초안` 같은 접두어를 노출하지 않는다.
- 생성 이미지 아래에 `이해를 돕기 위한 생성 이미지입니다.` 같은 반복 안내 문구를 넣지 않는다.
- 실제 아기 사진은 사용자가 원본 사용 또는 직접 편집을 명시하지 않으면 얼굴을 복제하지
  않는 참고 생성 방식으로만 사용한다.
- 사용자가 명시적으로 요청하지 않는 한 네이버 발행은 절대 하지 않는다. 자동화의 종료점은
  이미지가 포함된 임시저장과 결과 보고다.
- 필수 이미지가 누락되거나 사실 검증이 끝나지 않으면 불완전한 네이버 임시글을 새로 만들지
  말고 로컬 원고와 작업 자료를 보존한 뒤 누락 사항을 보고한다.
